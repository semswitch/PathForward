export function Technical() {
  return (
    <section className="mx-auto flex max-w-3xl flex-1 flex-col items-start justify-center gap-4 px-6 py-16">
      <h1 className="text-3xl font-semibold tracking-tight">How it works</h1>
      <p className="max-w-xl text-ink-muted">
        The full technical story — Microsoft Foundry agents, evidence-gated
        verification, and governed credential minting — lands here next.
      </p>
    </section>
  );
}
